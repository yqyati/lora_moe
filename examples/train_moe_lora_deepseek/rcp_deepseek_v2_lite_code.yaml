### model
model_name_or_path: /data/android/yqy/work/lora_moe/model/DeepSeek-V2-Lite
trust_remote_code: true
use_reentrant_gc: false

### method (RCP — DeepSeek-V2-Lite, code domain)
# DeepSeek-V2-Lite: 27 层(layer 0 dense + layer 1-26 MoE)
#   N_E=64 routed expert + 2 shared expert (always-on, frozen for PEFT)
#   Top-k routed=6, MLA attention
#
# 参数估算(同 math 域):
#   LoRA pool (N_L=256, rank=16): 16.78M
#   W_l + h_proj (per-layer × 26): 2.34M
#   总 trainable ≈ 19.12M
stage: sft
do_train: true
finetuning_type: moe_lora
moe_lora_n_experts: 256
moe_lora_rank: 16
moe_lora_alpha: 32
moe_lora_top_k: 6                       # 跟 base routed top-k=6 对齐
moe_lora_pool_share: global
moe_lora_w_share: per_layer
moe_lora_target_layers: all             # 自动覆盖 26 个 MoE block(layer 0 排除)
moe_lora_hidden_bottleneck_dim: 32

### dataset
dataset: magicoder_oss_75k
template: default
cutoff_len: 2048
preprocessing_num_workers: 16
dataloader_num_workers: 4

### output
output_dir: saves/deepseek_v2_lite/moe_lora_code/rcp
logging_steps: 5
save_steps: 100
plot_loss: true
overwrite_output_dir: true
save_only_model: true
report_to: wandb
run_name: deepseek_v2_lite_rcp_code

### train
per_device_train_batch_size: 16
gradient_accumulation_steps: 2
learning_rate: 4.0e-4
num_train_epochs: 1.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
gradient_checkpointing: true
ddp_find_unused_parameters: true

### deepspeed
deepspeed: examples/deepspeed/ds_z2_config.json
